// Inter Font Family - Export for use in React/Vite projects
import './inter.css'

export const interFontFamily = 'Inter, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif'

export const interWeights = {
  light: 300,
  regular: 400,
  medium: 500,
  semibold: 600,
  bold: 700,
  extrabold: 800,
}

export const interStyles = {
  normal: 'normal',
  italic: 'italic',
}

export default {
  family: interFontFamily,
  weights: interWeights,
  styles: interStyles,
}